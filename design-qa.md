# Design QA

## 비교 기준

- 승인 시안: `design/aster-ui-final-target.png`
- 최종 구현: `design/implementation-desktop-final.png`
- 통합 비교 입력: `design/qa-comparison-final.png`
- 상태: Preview, Web, Coral, Focus, Tokens 패널, 사람 검토 대기
- 크기: 양쪽 모두 1440 × 1024 CSS px이며, 구현 캡처는 선명한 문서 출력을 위해 device pixel ratio 2로 생성

두 화면을 같은 크기로 나란히 배치한 통합 비교 이미지에서 레이아웃, 타이포그래피, 여백, 테두리, 모서리 반경, 이미지 자르기, 상태 표현을 다시 검토했습니다.

## 검토 결과

현재 소스와 같은 리비전에서 자동 시각 회귀와 수동 비교를 다시 수행했으며, 기록한 검토 항목에서 차단 수준의 시각 또는 사용성 문제를 발견하지 않았습니다.

- 검은 상단 막대, 3단 워크스페이스, Coral 강조색, 파란색 포커스, 조밀한 트리, 큰 TreatmentCard, 오른쪽 검사 패널이라는 핵심 구성이 유지됩니다.
- 너비 264px의 탐색 영역, 유동형 본문, 너비 376px의 검사 패널, 높이 62px의 상단 막대, 높이 350px의 미리보기 프레임이 시안과 같은 계층을 유지합니다.
- 프로젝트에 포함된 Inter 가변 글꼴로 macOS와 Linux CI의 영문 렌더링 편차를 줄였습니다. 한국어는 Pretendard와 시스템 글꼴을 대체 글꼴로 사용합니다.
- 원본 800 × 1000 PNG 대신 400w와 800w WebP를 사용합니다. 최종 800w 파일은 33,812바이트이며 이미지 영역의 자르기와 밀도를 보존합니다.
- 실제 Chrome axe에서 발견한 Coral 동작 색상, 작은 회색 보조 텍스트, 성공 배지의 대비 문제를 수정했습니다.

## 시안과 의도적으로 다른 부분

다음 차이는 기능 또는 데이터 무결성을 위해 승인 시안의 표현을 교정한 결과입니다.

- `4 incoming changes`를 실제 테스트 픽스처 수와 같은 `3 Figma changes ready for review`로 변경했습니다.
- 존재하지 않던 브랜드 별칭을 실제 DTCG 별칭으로 변경하고, 접근 가능한 Coral 700을 사용했습니다.
- 실제 배포로 오해할 수 있는 `Publish`를 `Run rehearsal`로 변경했습니다.
- Coral과 Ocean을 확인할 수 있는 테마 선택기를 추가했습니다.
- iOS와 Android 컴포넌트 호환 표현을 제거하고 Swift와 Compose 토큰 산출물로 정확히 표시했습니다.
- 하드코딩된 품질 성공 문구를 저장소 보고서에서 생성한 근거 5개로 교체했습니다.
- 정적 임시 목록을 제거하고 매니페스트에서 생성한 실제 컴포넌트 6개만 노출합니다.

## 자동 시각 근거

설치된 Google Chrome에서 시각 기준 이미지를 갱신하지 않고 `pnpm test:visual`을 통과했습니다.

- 데스크톱 Coral 1440 × 1024
- 데스크톱 Ocean 1440 × 1024
- Figma 변경 검토 서랍 1440 × 1024
- 로컬 릴리스 리허설 대화 상자 1440 × 1024
- 200% 확대에 해당하는 뷰포트 720 × 512
- 모바일 Component Lab 390 × 844

같은 검증 스위트에서 실제 브라우저 axe 4회, 순환 탭 탐색, 연결 대상이 없는 `aria-controls` 부재, 가로 넘침, 모바일 서랍 패널, 강제 색상 모드의 포커스 표시를 확인합니다.

추가로 1280 × 720 데스크톱에서 동기화 상태 표시줄이 워크스페이스 경계를 넘지 않는지 검사합니다. 시각 비교 허용치는 전체 픽셀의 0.3%입니다.

시각 기준선은 저장소에 포함된 통과 상태의 검증 근거 픽스처를 명시적으로 주입해 실행 순서와 무관하게 같은 상태를 캡처합니다. Playwright 검증 근거 리포터는 별도로 현재 소스 리비전을 기록하며, 최종 Studio 빌드는 픽스처 재정의 없이 현재 리비전으로 근거를 다시 검증합니다.

## 수동 점검

- 기본 카드와 상태 카드 4개의 이미지가 영역을 벗어나지 않습니다.
- 테마 선택기를 추가해도 플랫폼 탭과 워크스페이스 탭이 겹치지 않습니다.
- 390px에서 상단 막대의 주요 동작, 검토 버튼, 테마, 플랫폼 제어가 유지되며 세로 스크롤로 전체 흐름을 완료할 수 있습니다.
- Figma 변경 검토 서랍과 릴리스 리허설 대화 상자가 화면 알림 위에 표시되고, 초점 고정과 복귀를 유지합니다.
- `Quality` 검사 패널의 5개 항목과 API 호환성 결과를 1024px 높이 안에서 읽을 수 있습니다.

## 남아 있는 차이와 외부 확인

- 인물 사진과 일부 임상 아이콘은 승인 시안의 독점 원본이 아니라 가장 가까운 실제 자산과 Phosphor 아이콘을 사용합니다.
- Studio는 로컬 저장소 검증과 GitHub Actions 실행을 구분합니다. 공개 CI와 Pages는 저장소 인증 후 별도 실행 근거로 확인합니다.

검토 결과: 기록된 자동 및 수동 확인 항목 통과
