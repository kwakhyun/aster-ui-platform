You are proposing a change to a shared React design-system package.

Read the request and current generated public API manifest below. Return one JSON object that conforms exactly to the supplied JSON Schema.

Rules:

- Do not claim that code, tests, Figma, CI, or a release was changed.
- Do not propose a required prop or a removal unless `semverImpact` is `major`.
- A new optional prop requires at least `minor` impact.
- Include concrete unit and accessibility verification.
- Include documentation changes, risks, and mitigations.
- Keep the proposal focused on one existing component.
- This is a proposal-only boundary. Human review is mandatory after validation.

REQUEST

{{REQUEST}}

CURRENT PUBLIC API MANIFEST

{{MANIFEST}}
